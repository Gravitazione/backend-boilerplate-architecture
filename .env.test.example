# Overrides for NODE_ENV=test (optional).
# Copy to .env.test and adjust. Loaded after .env (e.g. when running jest).

DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=postgres
DATABASE_PASSWORD=postgres
DATABASE_NAME=postgres_test

APP_URL=http://localhost:3000
PORT=3001
